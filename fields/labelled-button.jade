//- Labelled button
//- 
//- Offset the button so its inline with the rest of the fields. Leaving the
//- label column empty. Optionally add some help text on a new line.
//- 
//- Accepted arguments:
//- * helptext (String) [optional]
//- * field args (type, label)

include button

mixin labelled-button(args)
	.form-group
		.col-sm-9.col-sm-offset-3
			+button(args)(id=attributes.id,class=attributes.class)
			if args.helptext
				.help-block= args.helptext
